const Loading = () => {
  return (
    <div className="fixed top-0 left-0 bg-white w-full h-screen flex justify-center items-center gap-4 text-xl text-zinc-400 z-[999]">
      Loading...
    </div>
  );
};

export default Loading;
